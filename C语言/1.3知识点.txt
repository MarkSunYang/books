变量与算术表达式
for语句
符号常量
输入/输出
数组
函数
参数和值调用
字符数组
外部变量和作用域


2.函数和程序结构
函数的基本只是
返回非整形的函数
外部变量
作用域和规则
头文件
静态变量
寄存器变量
程序块结构
初始化
递归
C预处理器


指针：指针是程序数据在内存中的地址，而指针变量是用来保存这些地址的变量。

int* float* char* User*

内存是一个很大的，线性的字节数组（平坦寻址）。
每一个字节都是固定的大小，由8个二进制位组成。最关键的是，每一个字节都有一个唯一的编号,编号从0开始，一直到最后一个字节。
如上图中，这是一个256M的内存，他一共有256x1024x1024  = 268435456个字节，那么它的地址范围就是 0 ~268435455