1.对象：
1.1.对象的行为 behavior
1.2.对象的状态 state
1.3.对象的表示 identity

2.类之间的关系
2.1依赖 uses-a
2.2聚合(aggregation) has-a 
2.3继承 is-a

3.继承
接口实现
依赖
聚合
关联
直接关联、

4.使用预定义类：
4.1并不是所有的类都有面向对象的特征
4.2对象与对象变量
使用对象，就必须先构造对象，contructor来构造对象，new Object()
4.3getter和setter


讲到了构造函数，final，

1